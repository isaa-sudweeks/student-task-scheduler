# Production environment variables for Cloud Run (us-west2)
# Fill these in with your real values. Do not commit secrets.

# --- App Runtime ---
NODE_ENV=production

# Will be set automatically in CI on second deploy, but useful locally
NEXTAUTH_URL=https://<your-cloud-run-service-url>

# NextAuth and OAuth providers
NEXTAUTH_SECRET=<generate-a-strong-secret>
GOOGLE_CLIENT_ID=<google-oauth-client-id>
GOOGLE_CLIENT_SECRET=<google-oauth-client-secret>
# Optional GitHub provider
GITHUB_ID=<github-oauth-client-id>
GITHUB_SECRET=<github-oauth-client-secret>

# Optional cache (Upstash Redis)
REDIS_URL=<redis-url>
REDIS_TOKEN=<redis-token>

# --- Database (Cloud SQL Postgres) ---
# Raw credentials and connection settings
DB_USER=<db-user>
DB_PASS=<db-password>
DB_NAME=<db-name>

# Cloud SQL instance connection name: project:region:instance
INSTANCE_CONNECTION_NAME=<gcp-project-id>:us-west2:<instance-name>

# Prisma connection string for Cloud Run via Unix socket
# Replace placeholders above, or paste a fully resolved URL here
DATABASE_URL=postgresql://<db-user>:<db-password>@localhost:5432/<db-name>?host=/cloudsql/<gcp-project-id>:us-west2:<instance-name>&sslmode=disable

# --- GitHub Actions / GCP (reference) ---
# These are configured as GitHub Repository Variables/Secrets, not read from this file.
# Keeping them here as a single source of truth for production config.
GCP_REGION=us-west2
GCP_PROJECT_ID=<gcp-project-id>
AR_REPOSITORY=<artifact-registry-repo-name>
CLOUD_RUN_SERVICE=<cloud-run-service-name>

